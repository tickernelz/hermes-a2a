name: a2a
version: "0.3.3"
description: |
  A2A (Agent-to-Agent) protocol support. Receives messages from other agents
  via HTTP, injects into agent session via hooks. Provides /a2a slash command
  for status and agent management. No gateway patch needed.
requires_env: []
